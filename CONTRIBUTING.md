# 📋 提交规范指南

## 🎯 自动版本管理说明

本项目使用 **Semantic Release** 进行自动版本管理。系统会根据您的提交信息自动决定版本号递增类型：

## 📝 提交信息格式

```
<类型>(<范围>): <描述>

[可选的正文]

[可选的脚注]
```

### 🔖 提交类型说明

| 类型 | 版本递增 | 说明 | 示例 |
|------|----------|------|------|
| `feat` | **minor** | 新功能 | `feat: 添加用户登录功能` |
| `fix` | **patch** | 修复 Bug | `fix: 修复登录按钮无响应问题` |
| `perf` | **patch** | 性能优化 | `perf: 优化图片加载性能` |
| `refactor` | **patch** | 重构代码 | `refactor: 重构用户管理模块` |
| `security` | **patch** | 安全修复 | `security: 修复XSS漏洞` |
| `revert` | **patch** | 回滚更改 | `revert: 回滚用户登录功能` |

### 📋 不触发版本更新的类型

| 类型 | 说明 | 示例 |
|------|------|------|
| `docs` | 文档更新 | `docs: 更新README文档` |
| `style` | 代码格式 | `style: 修复代码缩进` |
| `test` | 测试相关 | `test: 添加用户登录测试` |
| `chore` | 构建/工具 | `chore: 更新依赖包` |
| `build` | 构建系统 | `build: 更新webpack配置` |
| `ci` | CI配置 | `ci: 更新GitHub Actions配置` |

### 🚨 重大更新 (BREAKING CHANGE)

任何包含 `BREAKING CHANGE:` 的提交都会触发 **major** 版本更新：

```bash
feat: 重新设计用户API

BREAKING CHANGE: 用户API返回格式已更改，需要更新客户端代码
```

## 🎯 示例提交

### ✅ 正确示例

```bash
# 新功能 (1.0.0 → 1.1.0)
feat: 添加全屏模式功能

# 修复问题 (1.0.0 → 1.0.1)  
fix: 修复全屏功能初始化错误

# 性能优化 (1.0.0 → 1.0.1)
perf: 优化启动加载时间

# 重大更新 (1.0.0 → 2.0.0)
feat!: 重新设计应用架构

BREAKING CHANGE: 应用配置文件格式已更改
```

### ❌ 错误示例

```bash
# 格式错误
修复bug
update readme
添加新功能

# 类型错误  
update: 修复登录问题
bugfix: 添加新功能
```

## 🚀 自动发布流程

1. **提交代码** → 系统分析提交信息
2. **自动构建** → 如需发布，自动构建应用
3. **生成版本** → 自动递增版本号并创建标签  
4. **发布Release** → 自动创建GitHub Release
5. **触发更新** → 已安装的应用会自动检测更新

## 🔍 版本检查

```bash
# 预览下次发布版本（不实际发布）
npm run release:dry

# 手动触发发布（通常不需要）
npm run release
```

## 💡 最佳实践

1. **一次提交一个功能/修复**
2. **使用清晰的描述**
3. **测试后再提交**
4. **重要更改添加详细说明**

---

遵循这些规范，您的每次提交都会被自动分析并在需要时创建新版本！🎉
